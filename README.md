# Command Calendar

This repository contains the **Command Calendar** SharePoint Framework solution.

Latest test package: **[Command-Calendar-V2.sppkg](Command-Calendar-V2.sppkg)** — **2.1.11.0**. Calendar directory links now use the actual full SharePoint calendar URL resolved at runtime. Replace and deploy the existing App Catalog package to upgrade; keep existing web part instances to retain their calendar configuration. See the [release notes and test steps](command-calendar-v2/README.md#release-21110--calendar-directory-urls).

## Where to go

All documentation, build instructions, and feature details for the current web part live here:

**[`command-calendar-v2/README.md`](command-calendar-v2/README.md)**

Use that folder for development (`npm install`, `npm run build`, `npm run package`) and for the App Catalog package at `command-calendar-v2/sharepoint/solution/command-calendar-v2.sppkg`.
